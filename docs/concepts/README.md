# docs/concepts/ — Conceptual Documentation

Tutorial-style explanations of BongoPay's core ideas (the payment lifecycle, simulation vs. real
providers, the provider adapter boundary) for a reader new to the project — as opposed to
[specs/](../../specs/README.md), which is normative and assumes familiarity with the domain.

## Status

**Empty — Phase 1+.** See [docs/README.md](../README.md) for the rules governing everything
under `docs/` (explain, don't redefine; link to `specs/` rather than restating it). No concept
doc exists yet; writing one ahead of the underlying spec settling (see
[ROADMAP.md](../../ROADMAP.md)) risks explaining something that's still `TODO(spec)`.
