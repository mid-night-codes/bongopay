# contracts/json-schema/payments/ — Payment Domain Schemas

JSON Schema for `Payment`, `PaymentRequest`, `PaymentResult`, `Money`, and the other concepts in
[specs/payments/payment-contract.md](../../../specs/payments/payment-contract.md). See
[contracts/json-schema/README.md](../README.md) for the rules governing this directory.

## Status

**Empty — Phase 1.** Blocked on the open `TODO(spec)`/`TODO(RFC)` items in
[specs/payments/payment-contract.md](../../../specs/payments/payment-contract.md) (e.g.
finalizing the `PaymentMethod` enum) — writing a schema against an unsettled spec would need
rework as soon as those resolve.
